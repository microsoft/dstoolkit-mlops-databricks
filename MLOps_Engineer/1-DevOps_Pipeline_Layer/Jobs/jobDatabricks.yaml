name: job
description: job

# Were we can define the inputs that our action will accept
inputs:
  version: 
    required: true
    description: "Test"
  namespace:
    required: true
    description: "Test"

runs:
  using: "composite"
  # Defining the action steps(Just one step to be simple)
  steps:
  - name: deploy stack
    uses: "./MLOps_Engineer/1-DevOps_Pipeline_Layer/Tasks/taskDatabricks.yaml"
    with:
      version: ${{ inputs.version }}
      namespace: ${{ inputs.namespace}}